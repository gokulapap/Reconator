setup:
  addons:
    - plan: heroku-postgresql:essential-0
build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  release: alembic upgrade head
  web: python -m app.container_entrypoint
  worker: python -m app.worker
